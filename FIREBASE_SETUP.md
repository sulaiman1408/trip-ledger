# ربط دفتر الرحلة بـ Firebase

هذا الدليل يشرح كيف تفعّل الحسابات (تسجيل دخول/تسجيل جديد) ومزامنة الرحلات
والمصاريف عبر السحابة، باستخدام مشروع Firebase خاص بك — **مجاناً بالكامل**
(بدون ربط بطاقة ائتمان أو خطة مدفوعة).

## 1. إنشاء المشروع

1. افتح [console.firebase.google.com](https://console.firebase.google.com)
   وأنشئ مشروعاً جديداً (أو استخدم مشروعاً موجوداً).
2. **Authentication** من القائمة الجانبية → تبويب **Sign-in method** → فعّل
   **Email/Password**.
3. **Firestore Database** → **Create database** → اختر أي منطقة قريبة منك
   → ابدأ بوضع **production mode**.
4. **Authentication → Settings → Authorized domains** → أضف:
   ```
   sulaiman1408.github.io
   ```
   (بدون هذه الخطوة، تسجيل الدخول يفشل على الموقع المنشور).

> **ملاحظة:** لا حاجة لتفعيل Firebase Storage — جوجل صارت تتطلب خطة Blaze
> المدفوعة له حتى ضمن الحصة المجانية، فالمرفقات (صور/PDF) تُحفظ بدلاً من
> ذلك مباشرة داخل بيانات كل رحلة في Firestore (بحد أقصى صغير لكل مرفق —
> انظر الملاحظات بالأسفل).

## 2. الحصول على بيانات الاعتماد (config)

1. ⚙️ **Project settings** (بجانب "Project Overview") → تبويب **General**.
2. تحت "Your apps" اضغط أيقونة الويب `</>` لإضافة تطبيق ويب (اسم أي شي، لا
   حاجة لـ Firebase Hosting).
3. سينسخ لك كائن `firebaseConfig` بهذا الشكل:
   ```js
   {
     apiKey: "...",
     authDomain: "...",
     projectId: "...",
     messagingSenderId: "...",
     appId: "..."
   }
   ```
4. افتح `index.html` وابحث عن `firebaseConfig` (بداية ملف الكود) واستبدل كل
   قيم `"REPLACE_ME"` بالقيم الحقيقية من مشروعك.

> هذا الكائن آمن تماماً لوضعه في كود الواجهة الأمامية (كل مستخدمي Firebase
> يفعلون هذا) — هو فقط يعرّف مشروعك، والحماية الفعلية تأتي من قواعد الأمان
> بالخطوة التالية، وليس من إخفاء هذه القيم.

## 3. تفعيل قواعد الأمان

**Firestore Database → Rules** → الصق محتوى ملف [`firestore.rules`](./firestore.rules)
من هذا المستودع → **Publish**.

هذه القواعد تضمن أن كل مستخدم يشوف ويعدّل رحلاته فقط (اللي هو منشئها أو
انضم لها عبر دعوة)، وتسمح بقبول دعوة رحلة (Invite) فقط لمن يملك رابط
الدعوة الصحيح — لا أحد غيره.

> إذا كنت رقّيت من نسخة سابقة كانت تخزّن الرحلات في `users/{uid}/trips`:
> هذا التحديث ينقل الرحلات لمجموعة جديدة (`trips` + `invites`) لدعم مشاركة
> الرحلة بين أكثر من حساب. لا يوجد نقل تلقائي للبيانات القديمة — أي رحلة
> تجريبية كانت موجودة قبل هذا التحديث تحتاج إعادة إنشاء.

## 4. الدفع والتجربة

بعد تعديل `firebaseConfig` والتزام (`commit`) التغيير ودفعه (`push`) إلى
الفرع، وبعد ما GitHub Pages ينشر النسخة الجديدة تلقائياً — افتح الموقع
وجرّب:
1. أنشئ حساباً جديداً (بريد + كلمة مرور 6 أحرف على الأقل).
2. أنشئ رحلة وأضف مصروفاً مع مرفق (صورة صغيرة أو PDF).
3. افتح نفس الرابط من متصفح آخر أو جهاز آخر وسجّل دخول بنفس الحساب — يفترض
   تشوف نفس البيانات فوراً (مزامنة مباشرة عبر Firestore).
4. تأكد إن المرفق يفتح بنجاح (معاينة داخل التطبيق للصور، تبويب جديد
   للملفات الأخرى).

## ملاحظات

- **كلمات المرور لا تُخزَّن أبداً في أي قاعدة بيانات** — Firebase
  Authentication يتولى تجزئتها (hashing) وتخزينها بأمان؛ التطبيق نفسه لا
  يرى أو يخزّن كلمة المرور الفعلية إطلاقاً.
- إذا كان عندك رحلات محفوظة محلياً على متصفحك من قبل تفعيل الحسابات، سيظهر
  لك عرض لاستيرادها لحسابك الجديد أول مرة تسجّل دخول (مرة واحدة فقط).
- **حجم المرفقات محدود** لأنها تُحفظ داخل مستند الرحلة نفسه في Firestore
  (بدون Storage): 250 كيلوبايت كحد أقصى لكل ملف، و650 كيلوبايت إجمالاً لكل
  رحلة (مجموع كل مرفقات كل مصاريفها). يظهر تنبيه واضح للمستخدم إذا تجاوز
  أحد هذين الحدين. يمكن تعديل الرقمين من `MAX_ATTACHMENT_BYTES` و
  `TRIP_ATTACHMENTS_BUDGET_BYTES` في `index.html` — لكن رفعهما كثيراً يخاطر
  بتجاوز حد Firestore الأقصى لحجم المستند الواحد (~1 ميجابايت)، مما يمنع
  حفظ أي تعديل جديد على تلك الرحلة حتى تُحذف مرفقات منها.
- كل رحلة تُحفظ كمستند Firestore منفصل (`trips/{tripId}`)، فحد الحجم أعلاه
  ينطبق على كل رحلة على حدة، وليس على كل رحلاتك مجتمعة.
- **دعوة المسافرين برقم الجوال**: عند إضافة مسافر برقم جوال، يظهر بجانبه
  زر "مشاركة دعوة" يفتح رابطاً جاهزاً عبر قائمة المشاركة بجوالك
  (واتساب/رسائل/أي تطبيق) — لأن التطبيق ثابت بدون سيرفر خاص، ما يقدر يرسل
  رسالة SMS تلقائياً بنفسه؛ أنت من يشارك الرابط. الشخص المدعو يفتح الرابط،
  يسجّل حساباً جديداً (أو يسجّل دخول إن كان عنده حساب)، ويدخل مباشرة على
  نفس الرحلة. رابط الدعوة يعمل لمرة واحدة فقط.
